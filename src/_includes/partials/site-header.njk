<header class="site-header">
    <nav>
        <ul>
            {% for navItem in navigation.items %}
                <li>
                    <a href="#{{ navItem.name | slugify }}">{{ navItem.name }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
</header>